const config = require('../config/app');
const nodemailer = require("nodemailer");
/**
* Fonction permettant d'envoyer des email
* @param req
* @param res
* @returns {*}
*/
function makeOrderCreation(req, res, next, orderCharge) {
 
}


module.exports = { makeOrderCreation };
